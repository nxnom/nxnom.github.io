{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "My portfolio website",
  "scripts": {
    "dev": "bundle exec jekyll serve --watch --livereload",
    "clean": "bundle exec jekyll clean",
    "build": "bundle exec jekyll build",
    "host:gh": "npm run build && gh-pages -d _site"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
